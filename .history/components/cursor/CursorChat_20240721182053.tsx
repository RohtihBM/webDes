import { CursorChatProps } from "@/types/type";
import React from "react";

const CursorChat = ({
  cursor,
  cursorState,
  setCursorState,
  updateMyPresense,
}: CursorChatProps) => {
  return <div></div>;
};

export default CursorChat;
